<div class="container">
  <div class="row">
    <%= form_for([@user, @comment]) do |ec| %>
      <div class="form-group">
        <%= ec.text_field :body, class: "form-control" %>
      </div>
        <%= ec.submit "Update Comment!" %>
    <% end %>
  </div>
</div>
